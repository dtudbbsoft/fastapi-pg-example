{
  "name": "@dtus/auth0",
  "version": "0.1.0",
  "private": true,
  "description": "FastAPI Auth0 module",
  "type": "module",
  "scripts": {
    "build": "poetry build",
    "lint": "poetry run ruff check .",
    "test": "poetry run coverage run -m pytest && poetry run coverage xml",
    "install:deps": "poetry install",
    "check-types": "poetry run mypy --strict ."
  }
}
